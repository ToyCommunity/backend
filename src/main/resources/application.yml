# 데이터 테스트를 위한 H2 db 연결

spring:
  jackson:
    default-property-inclusion: non_null
    serialization:
      fail-on-empty-beans: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shinity?autoReconnect=true
    username: ${db.username}
    password: ${db.password}
  #  datasource:
  #    url: jdbc:h2:tcp://localhost/~/toy
  #    driver-class-name: org.h2.Driver
  #    username: sa
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always
  redis:
    host: localhost
    port: 6379

logging:
  level:
    org.hibernate.type: trace

# 임시 주석 처리 추후 불필요시 제거
#jwt:
#  secret-key: ${JWT_SECRET_KEY}
#  access-token-expired-time: ${JWT_ACCESSTOKEN_EXPIRED_TIME_MS}
#  refresh-token-expired-time: ${JWT_REFRESHTOKEN_EXPIRED_TIME_MS}

